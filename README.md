# BrowserKit

Cross-platform framework for building fully custom desktop browsers using native system WebViews and a composited BrowserKit runtime.

BrowserKit does not bundle Electron. Wry is vendored because BrowserKit will eventually require compositor-specific patches. The project is currently in early architecture setup.
