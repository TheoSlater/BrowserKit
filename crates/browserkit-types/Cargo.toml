[package]
name = "browserkit-types"
version = "0.1.0"
edition = "2021"
description = "Platform-independent BrowserKit types"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"

[lib]
path = "src/lib.rs"
