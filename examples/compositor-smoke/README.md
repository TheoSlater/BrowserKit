# Linux compositor smoke

M0-B3 proof: two Linux composition-hosted pages, persistent frontend chrome, command routing,
repeated activation, and page destruction. Run with `cargo run -p compositor-smoke`.
